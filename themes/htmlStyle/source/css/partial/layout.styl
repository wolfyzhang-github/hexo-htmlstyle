html
    overflow-y: scroll

.body-container
    margin: 0 auto
    display: flex
    flex-direction: column
    align-items: center
    max-width: calc(640px + (100vw - 100%))
    overflow-x: hidden
    padding-left: calc(100vw - 100%)

.main-wrapper, .footer-wrapper
    width: 100%
    max-width: 640px  // 设置最大宽度